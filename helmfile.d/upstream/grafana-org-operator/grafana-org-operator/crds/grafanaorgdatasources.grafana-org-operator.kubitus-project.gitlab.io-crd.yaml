apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: grafanaorgdatasources.grafana-org-operator.kubitus-project.gitlab.io
spec:
  group: grafana-org-operator.kubitus-project.gitlab.io
  names:
    kind: GrafanaOrgDatasource
    listKind: GrafanaOrgDatasourceList
    plural: grafanaorgdatasources
    singular: grafanaorgdatasource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.url
      name: URL
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: GrafanaOrgDatasource is the Schema for the grafanaorgdatasources
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GrafanaOrgDatasourceSpec defines the desired state of GrafanaOrgDatasource.
            properties:
              access:
                description: access
                pattern: ^(proxy|direct)$
                type: string
              basicAuth:
                description: basic auth
                type: boolean
              basicAuthUser:
                description: basic auth user
                type: string
              database:
                description: database
                type: string
              envFrom:
                description: |-
                  list of sources to populate .env variable of templated data.
                  When a key exists in multiple sources, the value associated
                  with the last source will take precedence.
                items:
                  description: EnvFromSource represents the source of environment
                    variables.
                  properties:
                    secretRef:
                      description: the Secret to select from
                      properties:
                        name:
                          description: The Secret to select from.
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                      required:
                      - name
                      type: object
                  type: object
                type: array
              grafanaOrgRef:
                description: the name of the GrafanaOrg in the same namespace
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
                x-kubernetes-validations:
                - message: grafanaOrgRef is immutable
                  rule: self == oldSelf
              isDefault:
                description: is default
                type: boolean
              jsonData:
                description: json data
                type: object
                x-kubernetes-preserve-unknown-fields: true
              name:
                description: name
                type: string
              secureJsonData:
                additionalProperties:
                  type: string
                description: secure Json data
                type: object
              type:
                description: type
                type: string
              uid:
                description: uid
                type: string
              url:
                description: url
                type: string
              user:
                description: user
                type: string
              version:
                description: version
                format: int64
                type: integer
              withCredentials:
                description: with credentials
                type: boolean
            required:
            - grafanaOrgRef
            type: object
          status:
            description: GrafanaOrgDatasourceStatus defines the observed state of
              GrafanaOrgDatasource.
            properties:
              conditions:
                description: Represents the observations of a GrafanaOrgDatasource's
                  current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              orgID:
                description: The Org ID.
                format: int64
                type: integer
              orgName:
                description: The Org Name.
                type: string
              uid:
                description: uid
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
