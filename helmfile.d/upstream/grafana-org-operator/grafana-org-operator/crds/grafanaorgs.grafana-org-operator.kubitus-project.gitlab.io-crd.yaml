apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: grafanaorgs.grafana-org-operator.kubitus-project.gitlab.io
spec:
  group: grafana-org-operator.kubitus-project.gitlab.io
  names:
    kind: GrafanaOrg
    listKind: GrafanaOrgList
    plural: grafanaorgs
    singular: grafanaorg
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.orgName
      name: OrgName
      type: string
    - jsonPath: .spec.grafanaInstanceRef
      name: Instance
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: GrafanaOrg is the Schema for the grafanaorgs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GrafanaOrgSpec defines the desired state of GrafanaOrg.
            properties:
              grafanaInstanceRef:
                description: The name of the GrafanaInstance in the same namespace
                maxLength: 127
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]{0,61}[a-z0-9])?(/[a-z0-9]([-a-z0-9]{0,61}[a-z0-9])?)?$
                type: string
                x-kubernetes-validations:
                - message: grafanaInstanceRef is immutable
                  rule: self == oldSelf
              orgMapping:
                description: The Org Mappings
                items:
                  description: GrafanaOrgOrgMapping represents a mapping within the
                    Org.
                  properties:
                    group:
                      description: The name of the group in the authentication provider
                      pattern: ^[a-zA-Z0-9_-]+$
                      type: string
                    provider:
                      description: The name of the provider. All providers if not
                        set
                      type: string
                    role:
                      description: The name of the role given to the group
                      pattern: ^(None|Viewer|Editor|Admin)$
                      type: string
                  required:
                  - group
                  - role
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - group
                x-kubernetes-list-type: map
              orgName:
                description: The Org Name
                type: string
                x-kubernetes-validations:
                - message: orgName is immutable
                  rule: self == oldSelf
            required:
            - grafanaInstanceRef
            - orgName
            type: object
          status:
            description: GrafanaOrgStatus defines the observed state of GrafanaOrg.
            properties:
              conditions:
                description: Represents the observations of a GraganaOrg's current
                  state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              orgID:
                description: The Org ID.
                format: int64
                type: integer
              orgName:
                description: The Org Name.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
